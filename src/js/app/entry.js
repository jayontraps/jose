// var loadVideo = require('./modules/loadVideo');

$(document).ready(function () {
  // VIDEO OVERLAY...
  // var mq = window.matchMedia("(min-width: 1025px)");
  // mq.addListener(prepVideo);
  // prepVideo(mq);
  // function prepVideo(mq) {
  // 	if (mq.matches) {
  // 		$('#playBtn').show().on('click', loadVideo);
  // 	} else {
  // 		$('#playBtn').hide();
  // 	}
  // }
})
